language: python
# command to install dependencies
install: "pip install tox"
# command to run tests
script: tox -r
env:
  - TOXENV=flake8
  - TOXENV=py27
  - TOXENV=py33
  - TOXENV=py34
addons:
  postgresql: "9.3"
